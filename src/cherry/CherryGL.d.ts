import { CherryViewer3D } from '../types/cherry/CherryViewer3D';
import { CherryViewer } from '../types/cherry/CherryViewer';

/** @description Interfaces and Types for objects (assets files bundle CherryGL.data) and classes in 3dCore (ProjectManager, Object, Camera etc.) */
export default function Viewer3D({}: CherryViewer3D): Promise<CherryViewer>;
